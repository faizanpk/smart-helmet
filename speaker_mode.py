# speaker_mode.py – DEPRECATED
#
# The toggle-switch speaker mode has been replaced by the button-based call
# system (call_manager.py + live_call.py).
#
# This stub is kept so that any legacy import doesn't break.

import logging
log = logging.getLogger(__name__)
log.debug("[SPK] speaker_mode is deprecated. Call handling is done by call_manager.py.")
